
一、模型结构
大模型结构
小模型结构
编码器 共享空间（投影）
交互 位置编码（相加 rope）内容v 多头注意力（Wqkv QKV qk单一权重值 注意力掩码（掩码类别）softmax 标量乘v再同维度相加，从其他token交互信息） 残差连接 归一化 MLP（每个token独立经过同一mlp，提炼特征）  交互形式（CNN形式、纯编码器、纯解码器、编码解码器（qk模式、query模式 交叉注意力是会保留原来的v）交互到后面以该token理解的全文或上文 图像token是该铆钉下的一大块图像内容）
任务头 任务子空间（多个任务头将交互输出的通用特征，特征重组+归一化+非线性投影） 预测次数（密集预测、固定数量预测）

理解核心：下一层特征是上一层特征经过非线性的组合的一个更抽象的特征，权重模板是这个抽象特征的激活值。向量经过注意力的v矩阵抽象特征已经确定了，kv注意力就是让v的每一行相同抽象特征的强度融合。多头注意力是让关注角度不同，就有不同的v的抽象特征视角了。残差连接是让下一层模块只需要学习更高的抽象特征，以及反向传播时使用。MLP是将不同角度的抽象特征再一次组合并映射到更高更可分的维度空间，然后非线性降低维度组合出最重要的特征并和下一层输入维度一致，再一次经历qkv矩阵转换。kv缓存是因为只需理解上文，所以每一层输入下，前面的token输入到下一层的向量不变所以qkv都不变，但是只保留kv。


二、数据集、前向传播、损失函数设计、损失（正负样本设计、怎么分配不同任务的损失权重）、反向传播（会让不同子任务维度正交）、优化器、梯度下降、参数更新、学习率调度、权重验证、权重对比保存 计算图



三、前处理器、模型结构、权重加载、循环解码、后处理器 （auto 具体模型类 基类 模型文件 运行链路 模版链路）


四、模型格式、部署平台、省显存（模型权重、梯度、优化器两个、激活值、计算的中间值）、省核心、省读写


传感器（单目、双目、RGB-D、激光）、视觉定位、视觉重定位、三维重建（sfm（特征点描述子 特征匹配 外参 点云）、高斯、mvs 坐标转换（局部坐标系转换到某一个坐标系 局部坐标系的相机群和真实世界的相机坐标群有一个转换矩阵 然后把局部点全部转换到真实世界的点））、三维重建模型（根据模型去预测相机位姿、深度、点云，高斯模型还是训练出来的 也有图片输出直接出高斯模型的但是可能不是特别准）、校准（RTK、PPK）、Slam（建图、定位（相机的一系列位置轨迹就是外参）特征点匹配 运动恢复结构出现相机外参 然后出现点云 或者深度相机出现点云再外参 pnp算法）、自动驾驶（真实世界、传感器、感知、虚拟世界、跟踪预测（slam的相机轨迹外参能够把检测的物体放到同一个世界坐标系）、决策控制、控制模块执行）

