
一、模型结构
大模型结构
小模型结构
编码器 投影 共享空间
交互 位置编码（相加 rope）内容v 多头注意力【Wqkv QK 单一权重值 注意力掩码（类别 用在注意力后）】 残差连接 归一化 MLP  交互形式（CNN形式、纯编码器、纯解码器、编码解码器（qk模式、query模式 交叉注意力是会保留原来的v）交互到后面以该token理解的全文或上文 图像token是该铆钉下的一大块图像内容）
任务头 任务子空间（通用特征到任务特征（mlp、relu等操作）） 预测次数（密集预测、固定数量预测）
理解核心：下一层特征是上一层特征经过非线性的组合的一个更抽象的特征，权重模板是这个抽象特征的激活值。向量经过注意力的v矩阵抽象特征已经确定了，kv注意力就是让v的每一行相同抽象特征的强度融合。多头注意力是让关注角度不同，就有不同的v的抽象特征视角了。残差连接是让下一层模块只需要学习更高的抽象特征，以及反向传播时使用。MLP是将不同角度的抽象特征再一次组合并映射到更高更可分的维度空间，然后非线性降低维度组合出最重要的特征并和下一层输入维度一致，再一次经历qkv矩阵转换。kv缓存是因为只需理解上文，所以每一层输入下，前面的token输入到下一层的向量不变所以qkv都不变，但是只保留kv。


二、数据集、前向传播、损失函数设计、损失（多任务损失权重设置）、反向传播、梯度下降、优化器、参数更新、学习率调度、权重验证、权重对比保存 计算图



三、前处理器、模型结构、权重加载、循环解码、后处理器


四、模型格式、部署平台、省显存、省核心

